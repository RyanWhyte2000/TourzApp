apiVersion: v1
kind: Service
metadata:
  name: {{ include "tourz.fullname" . }}
  labels:
    {{- include "tourz.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tourz.selectorLabels" . | nindent 4 }}
